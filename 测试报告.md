## 1. 测试概览 (Executive Summary)

本次测试覆盖了 DormGo 系统的核心业务模块，主要包括用户认证、帖子管理、互动功能（点赞/评论/报名）以及信息流获取。

- **覆盖模块**：用户模块、帖子模块、消息模块、管理员模块。
- **核心关注**：接口连通性、参数校验逻辑、鉴权中间件 (`JWTAuthMiddleware`) 的有效性。

------

## 2. 详细测试用例 (Test Cases)

### 2.1 用户模块 (User Module)

此模块是系统的入口，重点测试注册与登录返回的 Token。

#### 2.1.1 用户注册

- **接口描述**: 新用户注册账号。

- **URL**: `POST /api/v1/user/signup`

- **Content-Type**: `application/json`

- **请求参数**:

  JSON

  ```
  {
      "username": "testuser001",
      "student_id": "20210001",
      "password": "password123",
      "re_password": "password123",
      "dorm_id": 1
  }
  ```

- **预期结果 (200 OK)**:

  JSON

  ```
  {
      "code": 200,
      "message": "Sign Up Success"
  }
  ```

- **异常测试**: 提交不一致的密码，应返回 `400` 错误。

#### 2.1.2 用户登录

- **接口描述**: 用户登录并获取 JWT Token。

- **URL**: `POST /api/v1/user/login`

- **Content-Type**: `application/json`

- **请求参数**:

  JSON

  ```
  {
      "username": "testuser001",
      "password": "password123"
  }
  ```

- **预期结果 (200 OK)**:

  JSON

  ```
  {
      "code": 200,
      "message": "Login Success",
      "token": "eyJhbGciOiJIUzI1NiIsInR5cCI...",  // <--- 重点：后续请求需携带此 Token
      "user": { ... }
  }
  ```

------

### 2.2 帖子模块 (Post Module)

帖子模块是核心功能，包含复杂的表单提交和图片上传。

#### 2.2.1 发布帖子 (核心)

- **接口描述**: 发布新帖子（支持图片上传）。

- **URL**: `POST /api/v1/post/create`

- **Header**: `Authorization: Bearer <Your_Token>`

- **Content-Type**: `multipart/form-data` (注意不是 JSON)

- **请求参数 (Form-Data)**: | Key | Value | Type | 必填 | 说明 | | :--- | :--- | :--- | :--- | :--- | | title | 宿舍夜宵搭子 | Text | 是 | 标题 | | content | 今晚有人一起吃烧烤吗？ | Text | 是 | 内容 | | publisherid | 10 | Text | 是 | 发布者ID | | dormid | 1 | Text | 是 | 宿舍楼ID | | typeid | 2 | Text | 是 | 帖子分区ID | | is_limited | true | Text | 否 | 是否限时/限额 | | max_enrollment| 5 | Text | 否 | 最大报名人数 | | deadline | 2026-02-01T12:00:00Z | Text | 否 | RFC3339 格式时间 | | images | (选择文件1.jpg) | File | 否 | 支持多图 | | images | (选择文件2.png) | File | 否 | 支持多图 |

- **预期结果 (200 OK)**:

  JSON

  ```
  {
      "code": 200,
      "msg": "Create Success"
  }
  ```

#### 2.2.2 获取帖子列表 (分页)

- **接口描述**: 获取首页信息流。

- **URL**: `GET /api/v1/post/posts`

- **Query Params**: `page=1`, `size=10`

- **预期结果 (200 OK)**:

  JSON

  ```
  {
      "code": 200,
      "data": [ { "id": 1, "title": "...", ... } ],
      "total": 100,
      "page": 1,
      "has_more": true
  }
  ```

#### 2.2.3 获取帖子详情

- **接口描述**: 点击进入帖子详情页，同时返回点赞、收藏状态。

- **URL**: `GET /api/v1/post/view/:id` (例如 `view/5`)

- **Header**: `Authorization: Bearer <Your_Token>`

- **预期结果 (200 OK)**:

  JSON

  ```
  {
      "code": 200,
      "data": { "id": 5, "title": "...", "Images": [...] },
      "isliked": false,
      "isFavorited": true,
      "is_pinned": false
  }
  ```

------

### 2.3 互动模块 (Interaction Module)

测试用户之间的交互行为。

#### 2.3.1 对帖子点赞

- **URL**: `POST /api/v1/post/postlike`

- **Header**: `Authorization: Bearer <Your_Token>`

- **Content-Type**: `application/json`

- **请求参数**:

  JSON

  ```
  {
      "postid": 5,
      "likerid": 10  // 当前用户ID
  }
  ```

- **预期结果**:

  JSON

  ```
  { "code": 200, "msg": "PostLike success" }
  ```

#### 2.3.2 发布评论

- **URL**: `POST /api/v1/post/comment`

- **Header**: `Authorization: Bearer <Your_Token>`

- **Content-Type**: `application/json`

- **请求参数**:

  JSON

  ```
  {
      "post_id": 5,
      "content": "算我一个！",
      "commenter_id": 10,
      "parent_id": 0  // 0表示根评论，非0表示回复某人
  }
  ```

- **预期结果**:

  JSON

  ```
  { "code": 200, "msg": "success" }
  ```

#### 2.3.3 活动报名

- **URL**: `POST /api/v1/post/signup`

- **Header**: `Authorization: Bearer <Your_Token>`

- **Content-Type**: `application/json`

- **请求参数**:

  JSON

  ```
  {
      "post_id": 5
  }
  ```

- **预期结果**:

  - 成功：`{ "code": 200, "msg": "报名成功" }`
  - 失败（重复报名）：`{ "code": 500, "msg": "你已经报过名了" }`
  - 失败（人数已满）：`{ "code": 500, "msg": "名额已满" }`

------

### 2.4 管理员模块 (Admin Module)

用于后台管理系统的测试。

#### 2.4.1 违规内容审核

- **URL**: `PUT /api/v1/admin/contents/:id/audit`

- **Header**: `Authorization: Bearer <Admin_Token>`

- **Content-Type**: `application/json`

- **请求参数**:

  JSON

  ```
  {
      "status": "approved", // 或 "rejected"
      "reason": "内容合规"
  }
  ```

- **预期结果**: 修改数据库中帖子的状态字段。

------

## 3. 风险与注意事项 (Risks & Notes)

1. **鉴权参数 (Authorization)**:
   - 除登录/注册/Ping接口外，几乎所有接口都在 `authPost` 或 `userAuth` 路由组下，测试时**必须**在 Header 中携带 `Authorization: Bearer <Token>`，否则会返回 `401 Unauthorized`。
2. **Form-Data 数据类型**:
   - `PostCreate` 接口在代码中明确使用了 `c.PostForm` 和 `c.MultipartForm`。在使用 Postman 测试时，**切勿使用 Raw JSON**，必须切换到 `Body -> form-data` 标签页进行测试。
3. **时间格式**:
   - 帖子发布接口的 `deadline` 字段解析使用了 `time.RFC3339` (例如 `2026-01-14T15:04:05Z`)。格式不正确会导致时间解析失败（代码中目前会忽略错误，需注意）。
4. **ID 类型**:
   - 代码中多处 ID 转换使用了 `strconv.Atoi` 或 `uint` 强转，测试时请确保 ID 为正整数，传递非数字字符串会导致 `400 Atoi failed`。

------

**建议**：在进行实际测试前，请确保 Redis 和 MySQL 服务已启动，并且 `config.yaml` 中的数据库连接配置正确。